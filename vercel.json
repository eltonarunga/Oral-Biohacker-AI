{
  "build": {
    "env": {
      "GEMINI_API_KEY": "@gemini_api_key"
    }
  },
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/static-build",
      "config": { "distDir": "dist" }
    }
  ]
}
